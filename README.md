# Prohibited Medication IPD Dashboard

Deploy either `app.py` or `prohibited-medication.py` as the Streamlit main file.

This patched version safely creates missing date columns before conversion, preventing KeyError during upload.
